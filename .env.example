# Пример параметров окружения для docker-compose.yml
# Скопируйте в .env и при необходимости измените:

POSTGRES_DB=vahta
POSTGRES_USER=vahta
POSTGRES_PASSWORD=vahta

# Порт, на котором будет доступен фронтенд (по умолчанию 80)
# WEB_PORT=80
